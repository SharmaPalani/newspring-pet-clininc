apiVersion: v1
kind: Service
metadata:
  name: pet-clinic-service
  namespace: default
  labels:
    app: pet-clinic
spec:
  selector:
    app: pet-clinic
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
  